game_over_stage = 0;

fade_in = 0;
fade_in_speed = 0.01;

lerpProgress = 0;
lerpSpeed =  0.01;


deadEndX = (camera_get_view_width(view_camera[0])/2);
deadEndY = (camera_get_view_height(view_camera[0])/2);
deadX = deadStartX;
deadY = deadStartY;
bufferTimer = -1;
